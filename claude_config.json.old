{
    "mcpServers": {
        "session-memory": {
            "command": "python",
            "args": [
                "/Users/christopherjoseph/.gemini/antigravity/playground/luminescent-cluster/session_memory_server.py"
            ],
            "description": "Session memory for git context and active files",
            "scope": "project"
        },
        "pixeltable-memory": {
            "command": "python",
            "args": [
                "/Users/christopherjoseph/.gemini/antigravity/playground/luminescent-cluster/pixeltable_mcp_server.py"
            ],
            "description": "Long-term organizational knowledge base",
            "scope": "project"
        }
    },
    "toolConfiguration": {
        "toolSearch": {
            "enabled": true,
            "provider": "regex"
        },
        "programmaticToolCalling": {
            "enabled": true
        },
        "deferredLoading": {
            "sessionMemory": false,
            "pixeltableMemory": true
        }
    },
    "comments": {
        "session-memory": "Always loaded - provides fast access to git history and current context",
        "pixeltable-memory": "Defer-loaded - only queried when needed for deep historical context",
        "toolSearch": "Enables on-demand tool discovery for 85% token reduction",
        "programmaticToolCalling": "Allows Claude to orchestrate complex queries without context pollution"
    }
}